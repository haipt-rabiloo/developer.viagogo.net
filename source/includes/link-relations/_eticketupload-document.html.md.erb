## eticketupload:document

You can GET the href of this link to download the original file that was
uploaded for an [`ETicketUpload`](#eticketupload). This endpoint requires the
`read:user` scope.


### Response

The file for an [`ETicketUpload`](#eticketupload). The response will have
content-type `application/pdf`.