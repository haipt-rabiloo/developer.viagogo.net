## viagogo:listingconstraints

~~~ shell
curl -X PUT "<%= ROOT['_links']['viagogo:listingconstraints']['href'] %>"
     -H "User-Agent: MyAwesomeApp"
     -H "Authorization: Bearer <%= ResourceHelpers::CLIENT_CREDENTIALS_TOKEN["access_token"] %>"
     -d '{"event":{"name":"<%= EMBEDDED_EVENT["name"] %>","start_date":"<%= EMBEDDED_EVENT["start_date"] %>"},"venue":{"name":"<%= EMBEDDED_VENUE["name"]%>","city":"<%= EMBEDDED_VENUE["city"] %>"},"country": {"code": "<%= COUNTRY["code"] %>"}}'
~~~

~~~ csharp
// TODO
~~~

~~~ ruby
# TODO
~~~

<%= json(:listing_constraints) %>

You can use a `PUT` request on the href of this link to retrieve the
[`ListingConstraints`](#listingconstraints) for an event. This endpoint requires
the `write:sellerlistings` and `write:requestedevents` scopes.


### Parameters

| **Name** | **Type** | **Required** | **Description** |
| `event` | [`Event`](#event) | Yes | The `name` and `start_date` of the event this listing is for. |
| `venue` | [`Venue`](#venue) | Yes | The `name` and `city` of the venue where the event is taking place. |
| `country` | [`Country`](#country) | Yes | The `code` of the country where the event is taking place. |

### Response

Returns the [`ListingConstraints`](#listingconstraints) resource for the
specified event.