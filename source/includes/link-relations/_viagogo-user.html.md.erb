## viagogo:user

<%= curl('GET', ROOT['_links']['viagogo:user']['href']) %>

~~~ csharp
var api = new ViagogoClient("clientId",
                            "clientSecret",
                            new ProductHeaderValue("MyAwesomeApp"));
var user = await api.User.GetAsync();
~~~

~~~ ruby
api = GogoKit::Client.new(client_id: CLIENT_ID,
                          client_secret: CLIENT_SECRET,
                          access_token: ACCESS_TOKEN)
# TODO
~~~

<%= json(:user) %>

You can `GET` the href of this link to retrieve the current authenticated user.


### Response

Returns the [`User`](#user) resource for the currently authenticated user.